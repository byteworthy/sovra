---
phase: 06-production-ready
plan: 05
type: execute
wave: 2
depends_on: [01]
files_modified:
  - packages/web/instrumentation.ts
  - packages/web/sentry.client.config.ts
  - packages/web/sentry.server.config.ts
  - packages/web/sentry.edge.config.ts
  - packages/web/next.config.ts
  - packages/web/components/providers/PostHogProvider.tsx
  - packages/web/lib/analytics/server.ts
  - packages/web/lib/analytics/events.ts
  - packages/web/app/layout.tsx
  - packages/web/lib/analytics/__tests__/analytics.test.ts
autonomous: true
requirements:
  - MON-01
  - MON-02
  - MON-04

must_haves:
  truths:
    - "Sentry captures errors in both server and client contexts"
    - "PostHog provider wraps the app and initializes on client"
    - "Server-side PostHog events flush immediately with flushAt:1, flushInterval:0"
    - "Custom events are defined for agent execution, tool usage, workspace runs"
    - "PostHog and Sentry degrade gracefully when env vars are absent"
  artifacts:
    - path: "packages/web/instrumentation.ts"
      provides: "Sentry server + edge initialization"
      exports: ["register", "onRequestError"]
    - path: "packages/web/components/providers/PostHogProvider.tsx"
      provides: "Client-side PostHog provider"
      min_lines: 15
    - path: "packages/web/lib/analytics/server.ts"
      provides: "Server-side event capture with immediate flush"
      exports: ["captureEvent"]
    - path: "packages/web/lib/analytics/events.ts"
      provides: "Custom event definitions"
      exports: ["EVENTS"]
  key_links:
    - from: "packages/web/app/layout.tsx"
      to: "packages/web/components/providers/PostHogProvider.tsx"
      via: "PostHogProvider wrapping children"
      pattern: "PostHogProvider"
    - from: "packages/web/lib/analytics/server.ts"
      to: "posthog-node"
      via: "PostHog Node client with flushAt:1"
      pattern: "flushAt.*1"
---

<objective>
Monitoring integration: Sentry error tracking, PostHog analytics, and custom event definitions for agent execution, tool usage, and workspace runs.

Purpose: Production observability -- errors tracked in Sentry, user behavior and system metrics tracked in PostHog.
Output: Sentry + PostHog fully wired with custom event catalog.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/06-production-ready/06-RESEARCH.md

@packages/web/next.config.ts
@packages/web/app/layout.tsx
@packages/web/package.json
</context>

<interfaces>
<!-- From Plan 01: @sentry/nextjs, posthog-js, posthog-node already installed -->
<!-- Existing next.config.ts needs withSentryConfig wrapping -->
<!-- Existing app/layout.tsx needs PostHogProvider wrapping -->
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Sentry integration (server, client, edge)</name>
  <files>
    packages/web/instrumentation.ts,
    packages/web/sentry.client.config.ts,
    packages/web/sentry.server.config.ts,
    packages/web/sentry.edge.config.ts,
    packages/web/next.config.ts
  </files>
  <read_first>
    packages/web/next.config.ts,
    packages/web/package.json
  </read_first>
  <behavior>
    - Test: Sentry client config initializes with NEXT_PUBLIC_SENTRY_DSN
    - Test: Sentry client config does not throw when DSN is empty (graceful degradation)
    - Test: instrumentation.ts exports register and onRequestError
  </behavior>
  <action>
    1. Write tests in `packages/web/lib/analytics/__tests__/analytics.test.ts` for Sentry config behavior. Mock @sentry/nextjs. Run -- expect RED.

    2. Create `packages/web/sentry.client.config.ts`:
       - Import * as Sentry from '@sentry/nextjs'
       - Only init if NEXT_PUBLIC_SENTRY_DSN is set (graceful degradation for self-hosters)
       - Sentry.init({ dsn, tracesSampleRate: 0.1, replaysSessionSampleRate: 0, replaysOnErrorSampleRate: 1.0 })
       - Low sample rates for open-source project (users control their own Sentry quotas)

    3. Create `packages/web/sentry.server.config.ts`:
       - Import * as Sentry from '@sentry/nextjs'
       - Only init if SENTRY_DSN is set
       - Sentry.init({ dsn: process.env.SENTRY_DSN, tracesSampleRate: 0.1 })

    4. Create `packages/web/sentry.edge.config.ts`:
       - Same pattern as server config for edge runtime

    5. Create `packages/web/instrumentation.ts`:
       - Export async function register(): imports sentry.server.config for nodejs runtime, sentry.edge.config for edge runtime
       - Import Sentry, export const onRequestError = Sentry.captureRequestError

    6. Update `packages/web/next.config.ts`:
       - Import withSentryConfig from '@sentry/nextjs'
       - Wrap existing config export with withSentryConfig
       - Options: org/project from env vars, silent when not CI
       - Add experimental.clientTraceMetadata: ['sentry-trace', 'baggage'] for Next.js 15

    7. Run tests -- expect GREEN.
  </action>
  <verify>
    <automated>cd /Users/kevinrichards/Projects/agentforge/packages/web && pnpm test -- --run lib/analytics/__tests__/analytics.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "Sentry.init" packages/web/sentry.client.config.ts
    - grep -q "NEXT_PUBLIC_SENTRY_DSN" packages/web/sentry.client.config.ts
    - grep -q "register" packages/web/instrumentation.ts
    - grep -q "onRequestError" packages/web/instrumentation.ts
    - grep -q "withSentryConfig" packages/web/next.config.ts
    - grep -q "clientTraceMetadata" packages/web/next.config.ts
    - Tests pass
  </acceptance_criteria>
  <done>Sentry fully wired for client, server, and edge. Graceful degradation when DSN absent. next.config.ts wrapped with Sentry. instrumentation.ts handles server/edge init.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: PostHog provider + server events + custom event catalog</name>
  <files>
    packages/web/components/providers/PostHogProvider.tsx,
    packages/web/lib/analytics/server.ts,
    packages/web/lib/analytics/events.ts,
    packages/web/app/layout.tsx,
    packages/web/lib/analytics/__tests__/analytics.test.ts
  </files>
  <read_first>
    packages/web/app/layout.tsx,
    packages/web/lib/analytics/__tests__/analytics.test.ts
  </read_first>
  <behavior>
    - Test: captureEvent creates PostHog client with flushAt:1, flushInterval:0
    - Test: captureEvent calls client.capture with correct params
    - Test: captureEvent calls client.shutdown() (awaitable)
    - Test: captureEvent does not throw when POSTHOG_KEY is absent
    - Test: EVENTS object has keys for agent_execution, tool_usage, workspace_run, api_key_created, subscription_changed
  </behavior>
  <action>
    1. Append tests to analytics.test.ts for PostHog server behavior. Mock posthog-node. Run -- expect RED.

    2. Create `packages/web/components/providers/PostHogProvider.tsx`:
       - 'use client' directive
       - Import posthog from 'posthog-js' and PostHogProvider from 'posthog-js/react'
       - useEffect: only init if NEXT_PUBLIC_POSTHOG_KEY is set
       - posthog.init(key, { api_host: NEXT_PUBLIC_POSTHOG_HOST || 'https://us.i.posthog.com', person_profiles: 'identified_only', capture_pageview: false })
       - NOTE: capture_pageview: false because App Router needs manual pageview tracking via usePathname
       - Wrap children in PHProvider

    3. Create `packages/web/lib/analytics/server.ts`:
       - Import PostHog from 'posthog-node'
       - Export async captureEvent(distinctId: string, event: string, properties?: Record<string, unknown>): Promise<void>
       - Guard: if !process.env.POSTHOG_KEY, return immediately (graceful degradation)
       - Create new PostHog client with flushAt: 1, flushInterval: 0 (required for serverless)
       - Call client.capture({ distinctId, event, properties })
       - Await client.shutdown() (MUST await or events dropped in serverless)

    4. Create `packages/web/lib/analytics/events.ts`:
       - Export const EVENTS with typed event names:
         agent_execution: 'agent_execution' (properties: agentId, model, durationMs, tokenCount)
         tool_usage: 'tool_usage' (properties: toolName, agentId, durationMs, success)
         workspace_run: 'workspace_run' (properties: workspaceId, mode, agentCount, durationMs)
         api_key_created: 'api_key_created' (properties: keyPrefix, permissions)
         api_key_revoked: 'api_key_revoked' (properties: keyPrefix)
         subscription_changed: 'subscription_changed' (properties: plan, previousPlan, event)
         billing_portal_opened: 'billing_portal_opened' (properties: plan)

    5. Update `packages/web/app/layout.tsx`:
       - Import PostHogProvider
       - Wrap children with PostHogProvider (inside existing providers)
       - Conditional: only render if NEXT_PUBLIC_POSTHOG_KEY exists (or let provider handle gracefully)

    6. Run tests -- expect GREEN.
  </action>
  <verify>
    <automated>cd /Users/kevinrichards/Projects/agentforge/packages/web && pnpm test -- --run lib/analytics/__tests__/analytics.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "flushAt.*1" packages/web/lib/analytics/server.ts
    - grep -q "flushInterval.*0" packages/web/lib/analytics/server.ts
    - grep -q "client.shutdown" packages/web/lib/analytics/server.ts
    - grep -q "POSTHOG_KEY" packages/web/lib/analytics/server.ts
    - grep -q "PostHogProvider" packages/web/app/layout.tsx
    - grep -q "capture_pageview.*false" packages/web/components/providers/PostHogProvider.tsx
    - grep -q "agent_execution" packages/web/lib/analytics/events.ts
    - grep -q "tool_usage" packages/web/lib/analytics/events.ts
    - grep -q "workspace_run" packages/web/lib/analytics/events.ts
    - All analytics tests pass
  </acceptance_criteria>
  <done>PostHog client provider wraps app, server-side capture uses immediate flush, custom event catalog covers agent execution, tool usage, workspace runs, and billing events. All tests green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client -> PostHog cloud | Analytics data sent to external PostHog host |
| Server -> Sentry | Error data sent to external Sentry |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-17 | Information Disclosure | PostHog events | mitigate | person_profiles: 'identified_only' prevents anonymous profile creation; no PII in event properties |
| T-06-18 | Information Disclosure | Sentry error reports | mitigate | Low sample rates; no sensitive data in error contexts; DSN is not a secret (public key) |
| T-06-19 | Denial of Service | PostHog server flush | accept | One client per request; await shutdown prevents goroutine leak; if PostHog is down, captureEvent returns without blocking |
</threat_model>

<verification>
1. Analytics tests pass: `cd packages/web && pnpm test -- --run lib/analytics`
2. PostHog flush pattern: `grep -r "flushAt" packages/web/lib/analytics/server.ts`
3. Sentry integration: `grep -r "withSentryConfig" packages/web/next.config.ts`
4. PostHog in layout: `grep -r "PostHogProvider" packages/web/app/layout.tsx`
5. TypeScript compiles: `cd packages/web && pnpm typecheck`
</verification>

<success_criteria>
- Sentry captures client, server, and edge errors
- PostHog provider wraps app with identified-only profiles
- Server-side events use immediate flush (flushAt:1, flushInterval:0) with await shutdown
- Custom events defined for agent execution, tool usage, workspace runs, billing
- Both systems degrade gracefully when env vars absent (self-hoster friendly)
- All tests green
</success_criteria>

<output>
After completion, create `.planning/phases/06-production-ready/06-05-SUMMARY.md`
</output>
