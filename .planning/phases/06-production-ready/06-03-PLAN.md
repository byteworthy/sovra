---
phase: 06-production-ready
plan: 03
type: execute
wave: 2
depends_on: [01]
files_modified:
  - packages/web/lib/admin/service-client.ts
  - packages/web/lib/admin/queries.ts
  - packages/web/lib/admin/__tests__/admin.test.ts
  - packages/web/app/(admin)/layout.tsx
  - packages/web/app/(admin)/admin/page.tsx
  - packages/web/app/(admin)/admin/tenants/page.tsx
  - packages/web/app/(admin)/admin/users/page.tsx
  - packages/web/app/(admin)/admin/audit/page.tsx
  - packages/web/app/(admin)/admin/system/page.tsx
  - packages/web/components/admin/AdminSidebar.tsx
  - packages/web/components/admin/AdminStatCard.tsx
  - packages/web/components/admin/TenantTable.tsx
  - packages/web/components/admin/UserTable.tsx
  - packages/web/components/admin/AuditLogTable.tsx
  - packages/web/components/admin/HealthCheckRow.tsx
  - packages/web/middleware.ts
  - packages/web/app/globals.css
autonomous: true
requirements:
  - ADMIN-01
  - ADMIN-02
  - ADMIN-03
  - ADMIN-04
  - ADMIN-05

must_haves:
  truths:
    - "Non-admin users are redirected away from /admin routes"
    - "Admin layout uses amber accent to distinguish from tenant scope"
    - "Admin can view list of all tenants with plan, user count, agent count"
    - "Admin can view list of all users across tenants"
    - "Admin can view audit logs filtered by severity"
    - "Admin can see system health status and platform stats"
    - "Service-role client is protected by server-only import guard"
  artifacts:
    - path: "packages/web/lib/admin/service-client.ts"
      provides: "Service-role Supabase client"
      exports: ["createAdminClient"]
    - path: "packages/web/lib/admin/queries.ts"
      provides: "Admin queries bypassing RLS"
      exports: ["getAllTenants", "getAllUsers", "getAuditLogs", "getPlatformStats"]
    - path: "packages/web/app/(admin)/layout.tsx"
      provides: "Admin layout with super-admin guard"
      min_lines: 20
    - path: "packages/web/components/admin/AdminSidebar.tsx"
      provides: "Admin navigation sidebar"
      min_lines: 30
  key_links:
    - from: "packages/web/app/(admin)/layout.tsx"
      to: "packages/web/lib/admin/service-client.ts"
      via: "import createAdminClient for is_platform_admin check"
      pattern: "createAdminClient"
    - from: "packages/web/middleware.ts"
      to: "is_platform_admin check"
      via: "admin path protection in middleware"
      pattern: "/admin"
---

<objective>
Admin dashboard: service-role client, admin queries, admin layout with super-admin guard, and all admin pages (overview, tenants, users, audit logs, system health).

Purpose: Platform operators need visibility into all tenants, users, and system health without being scoped to a single tenant.
Output: Complete admin dashboard at /admin with tenant management, user management, audit logs, and system overview.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/06-production-ready/06-RESEARCH.md
@.planning/phases/06-production-ready/06-UI-SPEC.md
@.planning/phases/06-production-ready/06-01-SUMMARY.md

@packages/web/middleware.ts
@packages/web/app/globals.css
@packages/web/lib/motion.ts
@packages/web/app/layout.tsx
@packages/web/app/(tenant)/t/[slug]/layout.tsx
@packages/web/components/tenant/Sidebar.tsx
</context>

<interfaces>
<!-- From Plan 01: supabase/migrations/20260412300000_phase6_admin.sql adds is_platform_admin boolean to users -->
<!-- Existing audit_logs table: id, tenant_id, action, actor_id, metadata, severity, created_at -->
<!-- Existing tenants table: id, name, slug, created_at, updated_at -->
<!-- Existing users table: id, email, full_name, avatar_url, created_at -->
<!-- Existing tenant_users table: tenant_id, user_id, role -->
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Admin service client + queries + middleware guard + tests</name>
  <files>
    packages/web/lib/admin/service-client.ts,
    packages/web/lib/admin/queries.ts,
    packages/web/lib/admin/__tests__/admin.test.ts,
    packages/web/middleware.ts
  </files>
  <read_first>
    packages/web/middleware.ts,
    packages/web/lib/auth/client.ts,
    packages/web/lib/tenant/resolver.ts
  </read_first>
  <behavior>
    - Test: createAdminClient creates Supabase client with SUPABASE_SERVICE_ROLE_KEY
    - Test: createAdminClient disables autoRefreshToken and persistSession
    - Test: getAllTenants returns array of tenants with user_count and agent_count
    - Test: getAllUsers returns array of users with tenant associations
    - Test: getAuditLogs returns paginated audit logs, filterable by severity
    - Test: getPlatformStats returns { totalTenants, activeUsers, totalAgents, apiCallsToday }
    - Test: middleware redirects non-admin users from /admin paths to /dashboard
    - Test: middleware allows admin users to access /admin paths
  </behavior>
  <action>
    1. Write ALL tests FIRST. Mock Supabase admin client. Run -- expect RED.

    2. Create `packages/web/lib/admin/service-client.ts`:
       - Add `import 'server-only'` as FIRST import (prevents client-side bundling of service role key)
       - Import createClient from @supabase/supabase-js
       - Export createAdminClient() returning Supabase client with:
         NEXT_PUBLIC_SUPABASE_URL, SUPABASE_SERVICE_ROLE_KEY
         auth: { autoRefreshToken: false, persistSession: false }
       - Throw if SUPABASE_SERVICE_ROLE_KEY is missing

    3. Create `packages/web/lib/admin/queries.ts`:
       - Import createAdminClient
       - Export getAllTenants(page=1, pageSize=20): query tenants table with count of tenant_users and agents per tenant. Return { data: Tenant[], total: number }.
       - Export getAllUsers(page=1, pageSize=20): query users joined with tenant_users. Return { data: User[], total: number }.
       - Export getAuditLogs(page=1, pageSize=50, severity?: string): query audit_logs with optional severity filter, ordered by created_at desc. Return { data: AuditLog[], total: number }.
       - Export getPlatformStats(): return { totalTenants, activeUsers (last 7 days), totalAgents, apiCallsToday (from tool_executions today) }.
       - Export suspendTenant(tenantId): update tenant status. Audit log: tenant.suspended.
       - Export deleteTenant(tenantId): delete tenant cascade. Audit log: tenant.deleted.

    4. Update `packages/web/middleware.ts`:
       - Add admin path detection: if pathname starts with '/admin'
       - For admin paths: check user session exists, then use service-role client to read is_platform_admin from users table
       - If not admin: redirect to /dashboard
       - If no session: redirect to /auth/login
       - Preserve existing tenant resolution logic for non-admin paths

    5. Run tests -- expect GREEN.
  </action>
  <verify>
    <automated>cd /Users/kevinrichards/Projects/agentforge/packages/web && pnpm test -- --run lib/admin/__tests__/admin.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "server-only" packages/web/lib/admin/service-client.ts
    - grep -q "SUPABASE_SERVICE_ROLE_KEY" packages/web/lib/admin/service-client.ts
    - grep -q "autoRefreshToken.*false" packages/web/lib/admin/service-client.ts
    - grep -q "getAllTenants" packages/web/lib/admin/queries.ts
    - grep -q "getAllUsers" packages/web/lib/admin/queries.ts
    - grep -q "getAuditLogs" packages/web/lib/admin/queries.ts
    - grep -q "getPlatformStats" packages/web/lib/admin/queries.ts
    - grep -q "/admin" packages/web/middleware.ts
    - grep -q "is_platform_admin" packages/web/middleware.ts
    - All admin tests pass
  </acceptance_criteria>
  <done>Service-role client guarded by server-only import. Admin queries bypass RLS for tenant/user/audit operations. Middleware enforces is_platform_admin for /admin paths. All tests green.</done>
</task>

<task type="auto">
  <name>Task 2: Admin layout + sidebar + overview page</name>
  <files>
    packages/web/app/(admin)/layout.tsx,
    packages/web/app/(admin)/admin/page.tsx,
    packages/web/components/admin/AdminSidebar.tsx,
    packages/web/components/admin/AdminStatCard.tsx,
    packages/web/components/admin/HealthCheckRow.tsx,
    packages/web/app/globals.css
  </files>
  <read_first>
    packages/web/app/(tenant)/t/[slug]/layout.tsx,
    packages/web/components/tenant/Sidebar.tsx,
    packages/web/app/globals.css,
    packages/web/lib/motion.ts,
    packages/web/lib/admin/queries.ts
  </read_first>
  <action>
    1. Add CSS animation to `packages/web/app/globals.css` (append to existing file):
       .key-reveal-highlight { animation: keyReveal 0.4s ease-out; }
       @keyframes keyReveal { 0% { background-color: rgba(59,130,246,0.2); } 100% { background-color: transparent; } }

    2. Create `packages/web/components/admin/AdminSidebar.tsx`:
       - Server component or client component matching existing Sidebar pattern
       - w-[220px] border-r border-border bg-background
       - "Platform" caption text at top (text-xs text-muted-foreground px-4 py-2)
       - Nav items (each h-[36px] px-3 rounded-md): Overview (LayoutDashboard), Tenants (Building2), Users (Users), Audit Logs (FileText), System (Activity)
       - Active state: bg-amber-500/15 text-amber-400 (AMBER not blue -- per UI spec)
       - Inactive hover: bg-zinc-800/40 text-foreground transition-colors duration-100
       - Use usePathname() for active detection

    3. Create `packages/web/app/(admin)/layout.tsx`:
       - Server component
       - Double guard: (1) middleware already checked, (2) layout also verifies is_platform_admin using createAdminClient
       - If not admin: redirect('/dashboard')
       - Render admin shell: header (h-14, border-b with border-amber-500/20) with "ByteSwarm Admin" label (text-amber-400) + user menu + "Exit admin" link
       - Sidebar (AdminSidebar) + main content area (flex-1 overflow-y-auto p-6)

    4. Create `packages/web/components/admin/AdminStatCard.tsx`:
       - Props: label, value, icon, delta (optional: { value: number, positive: boolean })
       - bg-card border border-border rounded-xl p-5
       - Top: label (text-xs text-muted-foreground) + icon (16px, right)
       - Value: text-2xl font-semibold mt-2
       - Delta: text-xs, green for positive, red for negative
       - hover: border-zinc-700 transition-colors duration-150

    5. Create `packages/web/components/admin/HealthCheckRow.tsx`:
       - Props: name, status ('healthy'|'degraded'|'down'), latencyMs (optional), lastChecked
       - py-3 flex items-center gap-3
       - Status dot: w-2 h-2 rounded-full (green/amber/red)
       - Service name: text-sm font-semibold
       - Status text: "Healthy"/"Degraded"/"Down" per UI spec copywriting
       - Latency: text-xs muted, right-aligned
       - CSS color transition 200ms on dot

    6. Create `packages/web/app/(admin)/admin/page.tsx`:
       - Server component
       - Call getPlatformStats() for stat cards
       - "Platform overview" heading (text-2xl font-semibold tracking-tight)
       - 4-col grid (responsive: 1-col mobile, 2-col tablet, 4-col desktop) of AdminStatCard:
         Total tenants | Active users | Total agents | API calls today
       - "System health" section with HealthCheckRow for: API server, Database, Go worker
       - Health data: call /api/health for self-check; for others use simple status display (real health checks wired in monitoring plan)
  </action>
  <verify>
    <automated>cd /Users/kevinrichards/Projects/agentforge/packages/web && pnpm typecheck</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "amber" packages/web/components/admin/AdminSidebar.tsx
    - grep -q "is_platform_admin" packages/web/app/(admin)/layout.tsx
    - grep -q "createAdminClient" packages/web/app/(admin)/layout.tsx
    - grep -q "server-only\|redirect" packages/web/app/(admin)/layout.tsx
    - grep -q "Platform overview" packages/web/app/(admin)/admin/page.tsx
    - grep -q "getPlatformStats" packages/web/app/(admin)/admin/page.tsx
    - grep -q "keyReveal" packages/web/app/globals.css
    - TypeScript compiles cleanly
  </acceptance_criteria>
  <done>Admin layout with amber nav accent, super-admin guard, overview page with stat cards and health status. All per UI spec design contract.</done>
</task>

<task type="auto">
  <name>Task 3: Admin tenant, user, and audit log pages</name>
  <files>
    packages/web/app/(admin)/admin/tenants/page.tsx,
    packages/web/app/(admin)/admin/users/page.tsx,
    packages/web/app/(admin)/admin/audit/page.tsx,
    packages/web/app/(admin)/admin/system/page.tsx,
    packages/web/components/admin/TenantTable.tsx,
    packages/web/components/admin/UserTable.tsx,
    packages/web/components/admin/AuditLogTable.tsx
  </files>
  <read_first>
    packages/web/lib/admin/queries.ts,
    packages/web/app/(admin)/admin/page.tsx,
    packages/web/components/admin/AdminSidebar.tsx,
    packages/web/lib/motion.ts
  </read_first>
  <action>
    1. Create `packages/web/components/admin/TenantTable.tsx`:
       - Client component for interactivity (suspend/delete actions)
       - Columns: Name (label + slug below), Plan (tier badge), Users (count), Agents (count), Created (date), Status (active/suspended), Actions (dropdown)
       - Per UI spec: h-[56px] rows, py-4 px-4, hover bg-zinc-900/40 transition-colors 100ms
       - Plan badges: free (gray), pro (blue), enterprise (purple) per color tokens
       - Row animation: listItem stagger 0.03s
       - Actions dropdown: "View", "Suspend" (with confirmation dialog per UI spec copy), "Delete" (red, with confirmation dialog per UI spec copy)
       - Pagination: rows 1-20 of N, Prev/Next buttons

    2. Create `packages/web/components/admin/UserTable.tsx`:
       - Client component
       - Columns: Name, Email, Tenants (comma-separated), Role, Created, Status
       - Same row styling as TenantTable
       - Pagination same pattern

    3. Create `packages/web/components/admin/AuditLogTable.tsx`:
       - Client component
       - Filter bar: severity pills (All | Info | Warning | Critical) per UI spec
       - Active pill: bg-zinc-700 text-foreground. Inactive: text-muted-foreground hover:text-foreground
       - Columns: Severity (icon, colored), Action, Actor (name, bold), Tenant, Timestamp
       - Row: py-3 px-4, dense view per UI spec
       - Expandable: click row to show JSON payload in code block (bg-zinc-900/60 p-3 text-xs font-mono)
       - Row animation: messageEnter for new entries
       - Pagination same pattern

    4. Create `packages/web/app/(admin)/admin/tenants/page.tsx`:
       - Server component
       - "Tenants" heading + search input + create tenant button per UI spec
       - Call getAllTenants() and pass to TenantTable
       - Use searchParams for page/search/filter

    5. Create `packages/web/app/(admin)/admin/users/page.tsx`:
       - Server component
       - "Users" heading
       - Call getAllUsers() and pass to UserTable

    6. Create `packages/web/app/(admin)/admin/audit/page.tsx`:
       - Server component
       - "Audit logs" heading + search + filter controls
       - Call getAuditLogs() and pass to AuditLogTable

    7. Create `packages/web/app/(admin)/admin/system/page.tsx`:
       - Server component
       - "System health" heading
       - Health check rows for all services
       - Custom metrics display (from PostHog when wired in Plan 05)
  </action>
  <verify>
    <automated>cd /Users/kevinrichards/Projects/agentforge/packages/web && pnpm typecheck</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "getAllTenants" packages/web/app/(admin)/admin/tenants/page.tsx
    - grep -q "getAllUsers" packages/web/app/(admin)/admin/users/page.tsx
    - grep -q "getAuditLogs" packages/web/app/(admin)/admin/audit/page.tsx
    - grep -q "Suspend" packages/web/components/admin/TenantTable.tsx
    - grep -q "listItem\|stagger" packages/web/components/admin/TenantTable.tsx
    - grep -q "severity" packages/web/components/admin/AuditLogTable.tsx
    - grep -q "amber\|warning\|critical" packages/web/components/admin/AuditLogTable.tsx
    - TypeScript compiles cleanly
  </acceptance_criteria>
  <done>All admin pages complete: tenants (CRUD with suspend/delete), users (list), audit logs (filterable by severity, expandable JSON), system health. All per UI spec with animations.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User -> /admin/* | Only is_platform_admin users can access; double-enforced at middleware + layout |
| Admin client -> Supabase | Service-role key bypasses all RLS; must never leak to client |
| Admin -> tenant data | Admin can CRUD all tenants; destructive actions require confirmation |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-10 | Elevation of Privilege | /admin routes | mitigate | Double guard: middleware checks is_platform_admin + layout checks again; service-role key protected by 'server-only' import |
| T-06-11 | Information Disclosure | service-role key | mitigate | 'server-only' import guard prevents client bundling; key only used in server components/actions |
| T-06-12 | Tampering | tenant suspension/deletion | mitigate | Confirmation dialogs; audit log entries for all destructive admin actions |
| T-06-13 | Repudiation | admin actions | mitigate | All admin actions logged to audit_logs table with actor_id, action, timestamp, metadata |
</threat_model>

<verification>
1. Admin tests pass: `cd packages/web && pnpm test -- --run lib/admin`
2. TypeScript compiles: `cd packages/web && pnpm typecheck`
3. Service-role guard: `grep -r "server-only" packages/web/lib/admin/service-client.ts`
4. Middleware guard: `grep -r "is_platform_admin" packages/web/middleware.ts`
5. Layout guard: `grep -r "is_platform_admin" packages/web/app/(admin)/layout.tsx`
</verification>

<success_criteria>
- Service-role client protected by server-only import
- Middleware + layout double enforcement of is_platform_admin
- Admin overview with 4 stat cards and health status
- Tenant management with suspend/delete + confirmations
- User management listing all users across tenants
- Audit log viewer with severity filtering and expandable JSON
- System health page
- Amber accent distinguishes admin from tenant scope
- All admin tests green, TypeScript clean
</success_criteria>

<output>
After completion, create `.planning/phases/06-production-ready/06-03-SUMMARY.md`
</output>
