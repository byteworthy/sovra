---
phase: 01-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/web/app/layout.tsx
  - packages/web/app/page.tsx
  - packages/web/components/ui/card.tsx
  - packages/web/next.config.ts
  - packages/shared/package.json
  - packages/shared/tsconfig.json
  - packages/shared/types/index.ts
  - packages/shared/types/database.ts
  - pnpm-workspace.yaml
  - Makefile
  - package.json
  - .env.example
  - .gitignore
autonomous: true
requirements: [FOUN-01, FOUN-02]

must_haves:
  truths:
    - "pnpm install succeeds from root without errors"
    - "Next.js dev server starts and renders the home page"
    - "TypeScript compiles without errors across web and shared packages"
    - "packages/shared is importable from packages/web"
  artifacts:
    - path: "packages/web/app/layout.tsx"
      provides: "Root layout with correct .tsx extension"
    - path: "packages/web/app/page.tsx"
      provides: "Home page with correct .tsx extension"
    - path: "packages/shared/package.json"
      provides: "Shared types package definition"
    - path: "packages/shared/types/index.ts"
      provides: "Type exports barrel file"
    - path: "pnpm-workspace.yaml"
      provides: "Workspace config listing web and shared only"
    - path: "Makefile"
      provides: "Go build/test/dev commands"
    - path: ".env.example"
      provides: "Documented env vars template"
  key_links:
    - from: "packages/web/tsconfig.json"
      to: "packages/shared/types/index.ts"
      via: "path alias @sovra/shared/*"
      pattern: "@sovra/shared"
---

<objective>
Fix broken scaffold file extensions, set up pnpm workspace for web + shared packages, create the shared types package, add Makefile for Go commands, and configure environment.

Purpose: The existing scaffold has broken .cts/.ctsx extensions that prevent JSX compilation, and the shared types package and Go build tooling are missing. This plan fixes the foundation so all subsequent plans build on a working monorepo.

Output: Working pnpm monorepo with web + shared packages, correct file extensions, Go Makefile, env template.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation/01-RESEARCH.md

Key facts from research:
- `.cts` = CommonJS TypeScript, incompatible with JSX. Must rename to `.tsx`/`.ts`
- `pnpm-workspace.yaml` currently uses `packages/*` glob which would include Go worker - must be explicit
- `packages/web/tsconfig.json` already has path alias for `@sovra/shared/*`
- There is a spurious `' components'` directory (with leading space) alongside `components` - delete it
- Do NOT upgrade Next.js past 15.x or Tailwind past v3
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix scaffold file extensions and clean up spurious directory</name>
  <files>
    packages/web/app/layout.tsx,
    packages/web/app/page.tsx,
    packages/web/components/ui/card.tsx,
    packages/web/next.config.ts
  </files>
  <action>
1. Delete the spurious `packages/web/ components` directory (has a leading space in the name). Use: `rm -rf "packages/web/ components"`

2. Rename broken extensions - use `git mv` to preserve history:
   - `git mv packages/web/app/layout.cts packages/web/app/layout.tsx`
   - `git mv packages/web/app/page.cts packages/web/app/page.tsx`
   - `git mv packages/web/components/ui/card.ctsx packages/web/components/ui/card.tsx`
   - `git mv packages/web/next.config.cts packages/web/next.config.ts`

3. Read each renamed file after renaming. Verify the content is valid JSX/TSX. If `layout.tsx` or `page.tsx` contain `export default` with JSX, they are correct. If `next.config.ts` uses `module.exports` (CommonJS), convert to `export default` (ESM):
   ```typescript
   import type { NextConfig } from 'next'
   const nextConfig: NextConfig = {}
   export default nextConfig
   ```

4. Verify no `.cts` or `.ctsx` files remain: `find packages/web -name "*.cts" -o -name "*.ctsx"` should return nothing.
  </action>
  <verify>
    <automated>cd /Users/kevinrichards/Projects/sovra && find packages/web -name "*.cts" -o -name "*.ctsx" | wc -l | grep -q "^0$" && echo "PASS: No broken extensions" || echo "FAIL"</automated>
  </verify>
  <done>Zero .cts/.ctsx files remain. All renamed files contain valid TypeScript/TSX content. Spurious space-prefixed directory deleted.</done>
</task>

<task type="auto">
  <name>Task 2: Create shared package and configure pnpm workspace</name>
  <files>
    packages/shared/package.json,
    packages/shared/tsconfig.json,
    packages/shared/types/index.ts,
    packages/shared/types/database.ts,
    pnpm-workspace.yaml,
    package.json,
    Makefile,
    .env.example,
    .gitignore
  </files>
  <action>
1. Update `pnpm-workspace.yaml` to explicitly list packages (not glob):
   ```yaml
   packages:
     - 'packages/web'
     - 'packages/shared'
   ```
   This excludes `packages/worker` (Go module, not a pnpm package).

2. Create `packages/shared/package.json`:
   ```json
   {
     "name": "@sovra/shared",
     "version": "0.0.0",
     "private": true,
     "main": "./types/index.ts",
     "types": "./types/index.ts",
     "exports": {
       ".": "./types/index.ts",
       "./types/*": "./types/*"
     }
   }
   ```

3. Create `packages/shared/tsconfig.json`:
   ```json
   {
     "compilerOptions": {
       "target": "ES2022",
       "module": "ESNext",
       "moduleResolution": "bundler",
       "strict": true,
       "esModuleInterop": true,
       "skipLibCheck": true,
       "declaration": true,
       "declarationMap": true,
       "outDir": "./dist"
     },
     "include": ["types/**/*.ts"]
   }
   ```

4. Create `packages/shared/types/index.ts`:
   ```typescript
   // Re-export all shared types
   export type { Database } from './database'
   ```

5. Create `packages/shared/types/database.ts` (stub - replaced by supabase gen types in Plan 03):
   ```typescript
   // Auto-generated by: supabase gen types typescript --local
   // Do not edit manually. Run `pnpm db:generate` to regenerate.
   //
   // Stub file - will be replaced after Supabase migrations are applied (Plan 03).

   export type Database = {
     public: {
       Tables: Record<string, never>
       Views: Record<string, never>
       Functions: Record<string, never>
       Enums: Record<string, never>
     }
   }
   ```

6. Update root `package.json` scripts - read the file first, then update the `scripts` section to:
   ```json
   {
     "scripts": {
       "dev": "pnpm -r --parallel dev",
       "build": "pnpm -r build",
       "lint": "pnpm -r lint",
       "test": "pnpm -r test",
       "type-check": "pnpm -r type-check",
       "go:build": "cd packages/worker && go build ./cmd/worker",
       "go:test": "cd packages/worker && go test ./...",
       "go:dev": "cd packages/worker && air -c .air.toml",
       "go:lint": "cd packages/worker && go vet ./...",
       "db:generate": "supabase gen types typescript --local > packages/shared/types/database.ts",
       "db:reset": "supabase db reset"
     }
   }
   ```
   Keep all other fields (name, version, private, description, packageManager, engines, keywords, license) unchanged.

7. Add `@sovra/shared` as a workspace dependency to `packages/web/package.json`:
   Run: `cd /Users/kevinrichards/Projects/sovra && pnpm --filter @sovra/web add @sovra/shared@workspace:*`

8. Create `Makefile` at project root:
   ```makefile
   .PHONY: go-build go-test go-dev go-lint go-tidy

   WORKER_DIR := packages/worker

   go-build:
   	cd $(WORKER_DIR) && go build -o bin/worker ./cmd/worker

   go-test:
   	cd $(WORKER_DIR) && go test ./... -v

   go-dev:
   	cd $(WORKER_DIR) && air -c .air.toml

   go-lint:
   	cd $(WORKER_DIR) && go vet ./...

   go-tidy:
   	cd $(WORKER_DIR) && go mod tidy
   ```
   NOTE: Makefile requires literal tabs for recipe lines, not spaces. Use tabs.

9. Create `.env.example` at project root:
   ```
   # Supabase (from `supabase status` after `supabase start`)
   NEXT_PUBLIC_SUPABASE_URL=http://localhost:54321
   NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key-from-supabase-status
   SUPABASE_SERVICE_ROLE_KEY=your-service-role-key-from-supabase-status

   # Database (direct connection for Go worker)
   DATABASE_URL=postgresql://postgres:postgres@localhost:54322/postgres

   # Go Worker
   HTTP_PORT=8080
   GRPC_PORT=50051
   GO_ENV=development

   # AI Providers (Phase 3+)
   # OPENAI_API_KEY=
   # ANTHROPIC_API_KEY=
   ```

10. Update `.gitignore` - read the existing file, then append these entries if not already present:
    ```
    # Go binaries
    packages/worker/bin/
    packages/worker/tmp/

    # Air hot reload
    /tmp/air/

    # Supabase local
    .supabase/

    # Environment
    .env
    .env.local
    .env*.local
    ```

11. Run `pnpm install` from root to link workspace packages.

12. Verify TypeScript compilation: `cd packages/web && npx tsc --noEmit`
  </action>
  <verify>
    <automated>cd /Users/kevinrichards/Projects/sovra && pnpm install --frozen-lockfile 2>/dev/null || pnpm install && cd packages/web && npx tsc --noEmit && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <done>
    - `pnpm install` succeeds with web + shared packages linked
    - `packages/shared` exists with package.json, tsconfig.json, and type stubs
    - `pnpm-workspace.yaml` lists only web and shared (not worker)
    - Root package.json has Go scripts and db scripts
    - Makefile exists with go-build, go-test, go-dev, go-lint targets
    - `.env.example` documents all required env vars
    - `.gitignore` covers Go binaries, air, supabase local state
    - TypeScript compiles without errors
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| .env files | Secrets stored locally, must not reach git |
| next.config.ts | Server-side config exposed to build, potential key leakage |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation |
|-----------|----------|-----------|-------------|------------|
| T-01-01 | I (Information Disclosure) | .env files | mitigate | `.gitignore` includes `.env`, `.env.local`, `.env*.local`. `.env.example` contains placeholder values only, never real keys. |
| T-01-02 | I (Information Disclosure) | next.config.ts | mitigate | Only `NEXT_PUBLIC_` prefixed vars exposed to client bundle. Server-only secrets (SUPABASE_SERVICE_ROLE_KEY, DATABASE_URL) never use NEXT_PUBLIC_ prefix. Documented in .env.example comments. |
| T-01-03 | T (Tampering) | package.json scripts | accept | Dev-only scripts, no production risk. Scripts execute local Go/pnpm commands. |
</threat_model>

<verification>
- `find packages/web -name "*.cts" -o -name "*.ctsx"` returns nothing
- `pnpm install` succeeds from root
- `cd packages/web && npx tsc --noEmit` exits 0
- `cat pnpm-workspace.yaml` shows web and shared only
- `ls packages/shared/types/` shows index.ts and database.ts
- `cat .env.example` documents all env vars
- `cat Makefile` has go-build, go-test, go-dev targets
</verification>

<success_criteria>
- All .cts/.ctsx files renamed to .ts/.tsx
- Shared package created and linked to web package
- pnpm workspace correctly scoped (no worker)
- TypeScript compiles cleanly
- Makefile provides Go build commands
- Environment template documents all required vars
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-01-SUMMARY.md`
</output>
