---
phase: 02-core-infrastructure
plan: 05
type: execute
wave: 3
depends_on:
  - 02-04
files_modified:
  - packages/web/app/(tenant)/t/[slug]/layout.tsx
  - packages/web/app/(tenant)/t/[slug]/dashboard/page.tsx
  - packages/web/app/(tenant)/t/[slug]/members/page.tsx
  - packages/web/app/invite/[token]/page.tsx
  - packages/web/app/onboarding/page.tsx
  - packages/web/components/tenant/sidebar.tsx
  - packages/web/components/tenant/sidebar-nav.tsx
  - packages/web/components/tenant/tenant-switcher.tsx
  - packages/web/components/tenant/invite-form.tsx
  - packages/web/components/tenant/member-row.tsx
  - packages/web/components/tenant/member-list.tsx
  - packages/web/components/onboarding/wizard.tsx
  - packages/web/components/ui/avatar.tsx
  - packages/web/components/ui/empty-state.tsx
  - packages/web/components/ui/toast-provider.tsx
  - packages/web/lib/toast.ts
  - packages/web/src/__tests__/tenant/tenant-provider.test.tsx
autonomous: false
requirements:
  - TEN-01
  - TEN-02
  - TEN-03
  - TEN-04
  - RBAC-02
  - RBAC-03
  - RBAC-04
  - AUTH-04

must_haves:
  truths:
    - "Tenant layout loads tenant from DB by slug, wraps children in TenantProvider"
    - "Sidebar has 240px width, zinc-900 bg, nav items with active left accent bar (before:absolute before:left-0 before:h-5 before:w-0.5 before:bg-primary)"
    - "Tenant switcher dropdown opens with dropdownEnter animation (spring 500/35)"
    - "Member list shows name, email, role badge, joined date with staggered listItem animation"
    - "Invite form expands with AnimatePresence height animation (300ms spring)"
    - "Toast notifications appear bottom-right with slide-up animation for async feedback"
    - "3-step onboarding wizard (create workspace -> invite team -> ready) with step slide transitions"
    - "Dashboard welcome card has gradient bg-gradient-to-br from-blue-500/10 to-violet-500/10 with fadeInUp entrance"
  artifacts:
    - path: "packages/web/app/(tenant)/t/[slug]/layout.tsx"
      provides: "Tenant layout with sidebar and TenantProvider"
    - path: "packages/web/components/tenant/sidebar.tsx"
      provides: "240px sidebar with nav, tenant switcher, user section"
    - path: "packages/web/components/tenant/tenant-switcher.tsx"
      provides: "Dropdown with AnimatePresence for switching tenants"
    - path: "packages/web/components/tenant/member-list.tsx"
      provides: "Permission-gated member list with staggered animation"
    - path: "packages/web/components/tenant/invite-form.tsx"
      provides: "Expandable invite form with email and link modes"
    - path: "packages/web/components/onboarding/wizard.tsx"
      provides: "3-step onboarding wizard with step transitions"
    - path: "packages/web/components/ui/toast-provider.tsx"
      provides: "Radix Toast provider with success/error/info variants"
  key_links:
    - from: "packages/web/app/(tenant)/t/[slug]/layout.tsx"
      to: "packages/web/lib/tenant/context.tsx"
      via: "wraps children in TenantProvider"
      pattern: "TenantProvider"
    - from: "packages/web/components/tenant/invite-form.tsx"
      to: "packages/web/lib/rbac/invitation.ts"
      via: "calls createInvitation and createInviteLink"
      pattern: "createInvitation"
    - from: "packages/web/components/tenant/member-list.tsx"
      to: "packages/web/lib/rbac/hooks.ts"
      via: "uses usePermission to gate actions"
      pattern: "usePermission"
    - from: "packages/web/components/onboarding/wizard.tsx"
      to: "packages/web/lib/tenant/actions.ts"
      via: "calls createTenant on step 1 completion"
      pattern: "createTenant"
---

<objective>
Build the tenant dashboard layout (sidebar, navigation, sticky header), tenant switcher, member management, invitation UI, onboarding wizard, and toast notification system with full UI-SPEC fidelity.

Purpose: Completes the tenant-facing UI layer. Users can navigate between tenants, manage members with RBAC-gated actions, accept invitations, and onboard through a guided wizard after signup. All async operations provide toast feedback.

Output: Tenant layout with sidebar, dashboard page, members page, invite acceptance page, onboarding wizard, tenant switcher, member list, invite form, toast system, avatar component, empty states.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-core-infrastructure/02-CONTEXT.md
@.planning/phases/02-core-infrastructure/02-RESEARCH.md
@.planning/phases/02-core-infrastructure/02-UI-SPEC.md
@.planning/phases/02-core-infrastructure/02-01-SUMMARY.md
@.planning/phases/02-core-infrastructure/02-02-SUMMARY.md
@.planning/phases/02-core-infrastructure/02-03-SUMMARY.md
@.planning/phases/02-core-infrastructure/02-04-SUMMARY.md

<interfaces>
<!-- From Plan 01 -->
From packages/web/lib/auth/client.ts:
```typescript
export function createSupabaseBrowserClient(): SupabaseClient<Database>
```

From packages/web/lib/auth/server.ts:
```typescript
export async function createSupabaseServerClient(): Promise<SupabaseClient<Database>>
```

<!-- From Plan 02 -->
From packages/web/lib/tenant/context.tsx:
```typescript
export function TenantProvider({ tenant, children }: { tenant: Tenant; children: ReactNode }): JSX.Element
export function useTenant(): { tenant: Tenant; tenantId: string; tenantSlug: string }
```

<!-- From Plan 03 -->
From packages/web/lib/rbac/invitation.ts:
```typescript
export async function acceptInvitation(token: string): Promise<{ tenantId: string | null; error: string | null }>
export async function createInvitation(input: CreateInvitationInput): Promise<InvitationResult>
export async function createInviteLink(tenantId: string, roleId: string, maxUses?: number, expiresInDays?: number): Promise<InvitationResult>
```

From packages/web/lib/rbac/hooks.ts:
```typescript
export function usePermission(action: string): { allowed: boolean; loading: boolean }
export function useRole(): { role: string | null; loading: boolean }
```

From packages/web/lib/tenant/actions.ts:
```typescript
export async function createTenant(input: { name: string; slug: string }): Promise<{ tenant: {...} | null; error: string | null }>
```

<!-- From Plan 04 -->
From packages/web/lib/motion.ts:
```typescript
export const TRANSITIONS: { default, spring, springFast, slow }
export const VARIANTS: { pageEnter, cardEnter, listItem, shake, dropdownEnter }
```

From packages/web/components/ui/skeleton.tsx:
```typescript
// className="animate-pulse rounded-md bg-zinc-800/60"
```

From packages/web/components/ui/spinner.tsx:
```typescript
// Loader2 with animate-spin, sm/md/lg sizes, aria-label="Loading"
```

From packages/web/components/auth/auth-card.tsx:
```typescript
// Glass morphism card with cardEnter animation -- reused for onboarding wizard
```

From packages/web/components/auth/auth-layout.tsx:
```typescript
// Split-screen layout -- reused for onboarding wizard
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Tenant layout + sidebar + dashboard + toast system + avatar + empty state</name>
  <files>packages/web/app/(tenant)/t/[slug]/layout.tsx, packages/web/app/(tenant)/t/[slug]/dashboard/page.tsx, packages/web/components/tenant/sidebar.tsx, packages/web/components/tenant/sidebar-nav.tsx, packages/web/components/tenant/tenant-switcher.tsx, packages/web/components/ui/avatar.tsx, packages/web/components/ui/empty-state.tsx, packages/web/components/ui/toast-provider.tsx, packages/web/lib/toast.ts, packages/web/src/__tests__/tenant/tenant-provider.test.tsx</files>
  <read_first>
    - packages/web/lib/tenant/context.tsx (TenantProvider, useTenant)
    - packages/web/lib/auth/server.ts (createSupabaseServerClient)
    - packages/web/lib/motion.ts (VARIANTS.dropdownEnter, VARIANTS.pageEnter, TRANSITIONS)
    - packages/web/components/ui/skeleton.tsx (Skeleton for loading states)
    - packages/web/components/ui/badge.tsx (existing Badge for roles)
    - packages/web/app/globals.css (CSS variables for reference)
    - .planning/phases/02-core-infrastructure/02-UI-SPEC.md (design contract -- source of truth)
  </read_first>
  <action>
**Toast system** -- create FIRST since other components use it:

**`packages/web/lib/toast.ts`:**
- Simple toast state manager using React context
- Types: `{ id: string; variant: 'success' | 'error' | 'info'; title: string; description?: string }`
- `useToast()` hook returning `{ toasts, toast(variant, title, description?), dismiss(id) }`
- Auto-dismiss after 4 seconds
- Max 3 visible toasts, oldest dismisses first

**`packages/web/components/ui/toast-provider.tsx`:**
- `'use client'` component
- Uses Radix Toast (`@radix-ui/react-toast` -- already in package.json)
- Position: bottom-right on desktop (`fixed bottom-4 right-4`), bottom-center on mobile (`fixed bottom-4 left-1/2 -translate-x-1/2 md:left-auto md:translate-x-0 md:right-4`)
- Toast variants from UI-SPEC:
  - success: `border-l-4 border-[#22C55E] bg-[#22C55E]/10` (--success token)
  - error: `border-l-4 border-[#EF4444] bg-[#EF4444]/10` (--destructive token)
  - info: `border-l-4 border-[#3B82F6] bg-[#3B82F6]/10` (--primary token)
- Animation: slide up from `y+16` + fade in, 250ms spring transition
- Each toast: `rounded-lg p-4 shadow-lg bg-card` with variant border-left
- Close button: X icon, `h-4 w-4 text-muted-foreground hover:text-foreground`
- Stacking: up to 3 visible, `space-y-2`
- Must wrap the app -- add to root layout or tenant layout

**Avatar** `packages/web/components/ui/avatar.tsx`:
- Props: `{ name: string; src?: string; size?: 'sm' | 'md' | 'lg' }`
- Sizes: sm=24px (`h-6 w-6 text-xs`), md=32px (`h-8 w-8 text-sm`), lg=40px (`h-10 w-10 text-base`)
- Image: `rounded-full object-cover`
- Initials fallback: first letter of each word in name, max 2 chars ("Kevin Richards" -> "KR")
- Fallback background: color-hashed from name -- one of 6 muted colors:
  `['bg-blue-500/20 text-blue-400', 'bg-violet-500/20 text-violet-400', 'bg-emerald-500/20 text-emerald-400', 'bg-amber-500/20 text-amber-400', 'bg-rose-500/20 text-rose-400', 'bg-cyan-500/20 text-cyan-400']`
- Hash function: sum of char codes modulo 6

**EmptyState** `packages/web/components/ui/empty-state.tsx`:
- Props: `{ icon: ReactNode; heading: string; body: string; action?: ReactNode }`
- Container: `flex flex-col items-center justify-center py-16 text-center`
- Icon wrapper: `bg-zinc-800 rounded-2xl p-4 text-primary mb-4`
- Heading: `text-lg font-semibold`
- Body: `text-sm text-muted-foreground max-w-xs mt-1`
- Action slot: `mt-4`

**Tenant layout** `packages/web/app/(tenant)/t/[slug]/layout.tsx`:
- Server component
- Reads `params.slug`, queries `tenants` table for matching slug via `createSupabaseServerClient()`
- If tenant not found: `notFound()` from `next/navigation`
- Maps DB row to `Tenant` type (camelCase)
- Wraps children in `<TenantProvider tenant={tenant}>`
- Renders `<Sidebar />` on the left + main content area on the right
- Main content: `flex-1 flex flex-col min-h-screen`
- Main header: `h-14 sticky top-0 z-10 flex items-center justify-between px-6 border-b border-border backdrop-blur-sm bg-background/80`
  - Left: Page title (from `children` or default "Dashboard")
  - Right: User avatar button (sign out on click)
  - Mobile: hamburger button (`md:hidden`) that opens sidebar as sheet overlay
- Page content wrapper: `p-6 max-w-5xl`
- Include `<ToastProvider>` wrapper

**Sidebar** `packages/web/components/tenant/sidebar.tsx`:
- `'use client'` component (needs interactive state)
- Container: `hidden md:flex flex-col w-60 bg-zinc-900 border-r border-border min-h-screen`
- Logo area (h-16): `flex items-center gap-2 px-4 h-16 border-b border-border`
  - Bot icon (`h-5 w-5 text-primary`) + "AgentForge" (`text-sm font-semibold text-foreground`)
- Tenant switcher button: below logo area, `px-3 py-2`
  - Shows current tenant name + `ChevronsUpDown` icon
  - `<TenantSwitcher />` opens on click
- Separator: `h-px bg-border mx-3 my-2`
- Nav section: `<SidebarNav />`
- User section (bottom): `mt-auto p-3 border-t border-border`
  - Avatar (sm) + user name/email + sign-out button

**SidebarNav** `packages/web/components/tenant/sidebar-nav.tsx`:
- `'use client'` component (needs pathname for active state)
- Uses `usePathname()` from `next/navigation`
- Nav items: Dashboard (LayoutDashboard icon), Agents (Bot icon), Members (Users icon), Settings (Settings icon)
- Nav item states from UI-SPEC:
  - Container: `relative flex h-10 items-center gap-2 rounded-md px-3 text-sm font-semibold transition-colors duration-150`
  - Default: `text-muted-foreground`
  - Hover: `hover:bg-zinc-800/50 hover:text-foreground`
  - Active: `bg-accent/10 text-primary` + left accent bar: `before:content-[''] before:absolute before:left-0 before:h-5 before:w-0.5 before:rounded-full before:bg-primary`
  - Active bar entrance: animated with `motion.div` -- height 0->h-5, opacity 0->1, 200ms spring transition
- "Agents" item: disabled in Phase 2, shows tooltip "Coming in next release" on hover via `title` attribute
- Links use `next/link` with `href={/t/${slug}/${path}}`

**TenantSwitcher** `packages/web/components/tenant/tenant-switcher.tsx`:
- `'use client'` component
- Fetches user's tenants via `supabase.from('tenant_users').select('tenants(id, slug, name)').eq('user_id', userId)` on mount
- Uses Radix DropdownMenu (`@radix-ui/react-dropdown-menu` -- already installed)
- Dropdown animation: wrap content in `AnimatePresence` + `motion.div` with `VARIANTS.dropdownEnter` (initial: opacity 0, scale 0.95, y -4; animate: opacity 1, scale 1, y 0; exit: same as initial; transition: spring stiffness 500, damping 35)
- Trigger: current tenant name + ChevronsUpDown icon, `flex items-center gap-2 w-full px-3 py-2 text-sm font-semibold rounded-md hover:bg-zinc-800/50 transition-colors`
- Each tenant row: `flex items-center gap-2 px-2 py-1.5 rounded-md cursor-pointer hover:bg-zinc-800/50 transition-colors duration-100`
  - Current tenant: `Check` icon (h-4 w-4 text-primary) before name
  - Other tenants: 16px spacer (no icon)
- Separator before bottom item
- Bottom item: `PlusCircle` icon + "New workspace" text, `text-muted-foreground hover:text-foreground`
  - On click: opens tenant creation dialog (Radix Dialog -- use existing `@radix-ui/react-dialog`)
  - Dialog content: heading "Create a workspace", workspace name input, slug preview, "Create workspace" CTA
  - Calls `createTenant()` on submit
  - On success: toast "Workspace created" (success variant) + `router.push('/t/${slug}/dashboard')`

**Dashboard page** `packages/web/app/(tenant)/t/[slug]/dashboard/page.tsx`:
- Server component with `metadata: { title: 'Dashboard | AgentForge' }`
- Welcome card spanning full content width:
  - `bg-gradient-to-br from-blue-500/10 to-violet-500/10 border border-border/50 rounded-xl p-8`
  - Entrance: `motion.div` with fadeInUp -- `initial={{ opacity: 0, y: 12 }}`, `animate={{ opacity: 1, y: 0 }}`, `transition={{ duration: 0.4, delay: 0.1, ease: [0.32, 0.72, 0, 1] }}`
  - Heading lg: "Welcome to {tenant_name}" -- `text-2xl font-semibold tracking-tight`
  - Supporting: "Get started by creating your first agent or inviting your team." -- `text-sm text-muted-foreground mt-2`
  - Two ghost CTAs in `flex gap-3 mt-6`:
    - "Create an agent" (disabled, `opacity-50 cursor-not-allowed`, tooltip "Coming in next release") -- `Button variant="ghost"` with `ArrowRight` icon
    - "Invite team members" (links to `/t/{slug}/members`) -- `Button variant="ghost"` with `ArrowRight` icon
- Below welcome card: 3 placeholder stat cards in `grid grid-cols-1 md:grid-cols-3 gap-4 mt-6`
  - Each: `bg-card rounded-xl border border-border p-6`
  - Title: "Agents" / "Workspaces" / "Conversations" in `text-sm text-muted-foreground`
  - Value: "0" in `text-2xl font-semibold mt-1`
  - Note: `text-xs text-muted-foreground mt-1` -- "Coming soon"

**TenantProvider render test** `packages/web/src/__tests__/tenant/tenant-provider.test.tsx`:
- Test: TenantProvider renders children
- Test: useTenant returns tenant data within provider
- Test: useTenant throws outside provider
- Mock Supabase client
  </action>
  <verify>
    <automated>cd /Users/kevinrichards/Projects/agentforge/packages/web && npx tsc --noEmit 2>&1 | tail -10 && npm test -- --reporter=verbose tenant/tenant-provider 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - packages/web/app/(tenant)/t/[slug]/layout.tsx contains `TenantProvider`
    - packages/web/app/(tenant)/t/[slug]/layout.tsx contains `createSupabaseServerClient`
    - packages/web/app/(tenant)/t/[slug]/layout.tsx contains `notFound()`
    - packages/web/app/(tenant)/t/[slug]/layout.tsx contains `Sidebar`
    - packages/web/app/(tenant)/t/[slug]/layout.tsx contains `backdrop-blur`
    - packages/web/app/(tenant)/t/[slug]/layout.tsx contains `sticky`
    - packages/web/components/tenant/sidebar.tsx contains `w-60`
    - packages/web/components/tenant/sidebar.tsx contains `bg-zinc-900`
    - packages/web/components/tenant/sidebar.tsx contains `TenantSwitcher`
    - packages/web/components/tenant/sidebar-nav.tsx contains `before:absolute`
    - packages/web/components/tenant/sidebar-nav.tsx contains `before:bg-primary`
    - packages/web/components/tenant/sidebar-nav.tsx contains `before:h-5`
    - packages/web/components/tenant/sidebar-nav.tsx contains `before:w-0.5`
    - packages/web/components/tenant/sidebar-nav.tsx contains `hover:bg-zinc-800/50`
    - packages/web/components/tenant/sidebar-nav.tsx contains `bg-accent/10 text-primary`
    - packages/web/components/tenant/tenant-switcher.tsx contains `AnimatePresence`
    - packages/web/components/tenant/tenant-switcher.tsx contains `dropdownEnter`
    - packages/web/components/tenant/tenant-switcher.tsx contains `tenant_users`
    - packages/web/components/tenant/tenant-switcher.tsx contains `New workspace`
    - packages/web/components/tenant/tenant-switcher.tsx contains `createTenant`
    - packages/web/app/(tenant)/t/[slug]/dashboard/page.tsx contains `Welcome to`
    - packages/web/app/(tenant)/t/[slug]/dashboard/page.tsx contains `from-blue-500/10 to-violet-500/10`
    - packages/web/app/(tenant)/t/[slug]/dashboard/page.tsx contains `Invite team members`
    - packages/web/components/ui/toast-provider.tsx contains `border-l-4`
    - packages/web/components/ui/toast-provider.tsx contains `#22C55E`
    - packages/web/components/ui/toast-provider.tsx contains `#EF4444`
    - packages/web/components/ui/avatar.tsx contains `rounded-full`
    - packages/web/components/ui/avatar.tsx contains `bg-blue-500/20`
    - packages/web/components/ui/empty-state.tsx contains `py-16`
    - packages/web/src/__tests__/tenant/tenant-provider.test.tsx passes
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>Tenant layout with sidebar (240px, zinc-900, nav with active accent bar), sticky backdrop-blur header, tenant switcher with dropdown animation, dashboard welcome card with gradient, toast system with success/error/info variants, avatar with initials fallback, empty state component. TenantProvider test passes.</done>
</task>

<task type="auto">
  <name>Task 2: Member management + invite page + onboarding wizard</name>
  <files>packages/web/app/(tenant)/t/[slug]/members/page.tsx, packages/web/app/invite/[token]/page.tsx, packages/web/app/onboarding/page.tsx, packages/web/components/tenant/invite-form.tsx, packages/web/components/tenant/member-row.tsx, packages/web/components/tenant/member-list.tsx, packages/web/components/onboarding/wizard.tsx</files>
  <read_first>
    - packages/web/lib/rbac/invitation.ts (createInvitation, createInviteLink, acceptInvitation)
    - packages/web/lib/rbac/hooks.ts (usePermission, useRole)
    - packages/web/lib/tenant/actions.ts (createTenant)
    - packages/web/lib/tenant/context.tsx (useTenant)
    - packages/web/lib/motion.ts (VARIANTS.listItem, TRANSITIONS)
    - packages/web/lib/toast.ts (useToast -- just created in Task 1)
    - packages/web/components/ui/avatar.tsx (Avatar -- just created)
    - packages/web/components/ui/empty-state.tsx (EmptyState -- just created)
    - packages/web/components/auth/auth-layout.tsx (AuthLayout -- reuse for onboarding)
    - packages/web/components/auth/auth-card.tsx (AuthCard -- reuse for onboarding)
    - .planning/phases/02-core-infrastructure/02-UI-SPEC.md (design contract -- source of truth)
  </read_first>
  <action>
**MemberRow** `packages/web/components/tenant/member-row.tsx`:
- `'use client'` component
- Props: `{ member: { userId, name, email, avatarUrl, role, joinedAt }; canManage: boolean; onRemove: (userId: string) => void; onRoleChange: (userId: string, roleId: string) => void }`
- Row: `flex items-center gap-3 px-4 h-14 hover:bg-zinc-800/30 transition-colors duration-100 rounded-md`
- Entrance: `motion.div` with `VARIANTS.listItem` (opacity 0->1, x -8->0, 200ms)
- Layout:
  - Avatar (md=32px) + Name/Email column (`flex flex-col`: name in `text-sm font-semibold`, email in `text-xs text-muted-foreground`)
  - Role: `<Badge variant="secondary">` with role name
  - Joined: `text-xs text-muted-foreground` formatted date
  - Actions (if canManage): `MoreHorizontal` icon button -> Radix DropdownMenu
    - "Change role" -> sub-menu with role options
    - "Remove member" -> triggers inline confirmation
- Inline remove confirmation (NO modal -- per UI-SPEC):
  - Row expands: "Remove **{name}**? They'll lose all access to this workspace immediately."
  - Two buttons: "Remove" (`variant="destructive" size="sm" className="h-8 text-xs"`) + "Cancel" (`variant="ghost" size="sm" className="h-8 text-xs"`)
  - On remove: row fades out (`opacity 1->0, height auto->0`, 250ms), toast "Removed {name}"
  - On cancel: row returns to normal (reverse animation)

**MemberList** `packages/web/components/tenant/member-list.tsx`:
- `'use client'` component
- Uses `usePermission('member:read')` -- only renders content if allowed
- Uses `useTenant()` for tenantId
- Fetches members: `supabase.from('tenant_users').select('*, users(email, full_name, avatar_url), roles(name)').eq('tenant_id', tenantId)`
- While loading: member list skeleton from UI-SPEC:
  - 3 rows, each `h-14 flex gap-3 items-center px-4`
  - Each row: `<Skeleton className="w-8 h-8 rounded-full" />` (avatar) + `flex flex-col gap-1.5`: `<Skeleton className="w-32 h-3" />` + `<Skeleton className="w-24 h-2.5" />` + `<Skeleton className="w-16 h-5 rounded-full ml-auto" />` (badge)
- Stagger animation: `motion.div` wrapper with `staggerChildren: 0.05` on the list container
- Each member: `<MemberRow>` with `canManage={usePermission('member:manage').allowed}`
- If no members besides current user: `<EmptyState icon={<Users />} heading="Just you here" body="Invite teammates to collaborate on AI agents." action={<Button>Invite your first member</Button>} />`

**InviteForm** `packages/web/components/tenant/invite-form.tsx`:
- `'use client'` component
- Uses `usePermission('tenant:invite')` -- only renders if allowed
- Expandable: toggles open/closed with `AnimatePresence` + `motion.div`:
  - `initial={{ height: 0, opacity: 0 }}`, `animate={{ height: 'auto', opacity: 1 }}`, `exit={{ height: 0, opacity: 0 }}`
  - `transition: TRANSITIONS.spring` (stiffness 400, damping 30)
- Email invite mode:
  - Card container: `bg-card border border-border rounded-lg p-4`
  - Layout: `flex gap-3 items-end`
  - Email input (`flex-1`), Role select (`w-40`, uses Radix Select with roles fetched from DB), "Send invitation" button
  - On submit: calls `createInvitation({ tenantId, roleId, email, inviteType: 'email' })`
  - Success: toast "Invitation sent to {email}" (success variant, #22C55E), form collapses
  - Error: inline error below email input (`text-xs text-destructive`)
- Invite link mode:
  - Below email form: "Or share invite link" toggle, `text-sm text-muted-foreground hover:text-foreground cursor-pointer`
  - When toggled: shows row with `flex gap-2 items-center mt-3`
    - Role select + "Generate link" button
    - After generation: readonly input with link + Copy icon button
    - Copy click: icon swaps to `CheckCircle2` for 2s, toast "Invite link copied to clipboard" (success variant)
  - Calls `createInviteLink(tenantId, roleId)` on generate

**Members page** `packages/web/app/(tenant)/t/[slug]/members/page.tsx`:
- Server component with `metadata: { title: 'Team members | AgentForge' }`
- Page header: "Team members" (`text-2xl font-semibold tracking-tight`) + Badge showing member count
- Action bar: "Invite member" button (`variant="default"`, right-aligned, `flex justify-between items-center`)
  - On click: toggles InviteForm expansion
- `<InviteForm />` below action bar
- `<MemberList />` below invite form

**Invite acceptance page** `packages/web/app/invite/[token]/page.tsx`:
- `'use client'` page
- Uses `AuthLayout` + `AuthCard` for consistent visual treatment
- On mount: calls `acceptInvitation(token)` server action
- States:
  - Loading: Spinner centered in AuthCard, `aria-busy="true"`
  - Success:
    - Heading: "Welcome to {tenant name}" (`text-2xl font-semibold tracking-tight`)
    - Body: "You now have access as **{role}**"
    - CTA: "Open your dashboard" button -> `router.push('/t/{slug}/dashboard')`
    - Success animation: `CheckCircle2` icon with `motion.div` scale spring (0->1, stiffness 400, damping 30)
  - Error (expired): Heading "This invitation has expired", Body "Ask the workspace owner to send you a new invitation."
  - Error (invalid): Heading "Invitation not found", Body "This link may have already been used or is no longer valid."
  - Error (not authenticated): redirect to `/login?next=/invite/{token}`

**Onboarding wizard** `packages/web/components/onboarding/wizard.tsx`:
- `'use client'` component
- 3-step wizard in same split-screen layout as auth pages (reuse `AuthLayout` + `AuthCard`)
- Step indicator: 3 dots at top of card, filled dot = current step
  - Dots: `h-2 w-2 rounded-full transition-colors duration-200`
  - Active: `bg-primary`
  - Inactive: `bg-zinc-700`
  - Container: `flex justify-center gap-2 mb-6`
- Progress bar: thin 2px line at top of card, width animated 33%->66%->100%
  - `<motion.div className="h-0.5 bg-primary rounded-full" animate={{ width: `${step * 33.33}%` }} transition={TRANSITIONS.spring} />`
- Step transitions: `AnimatePresence mode="wait"` with slide:
  - Exit current: `translateX(0 -> -100%)` + opacity 1->0
  - Enter next: `translateX(100% -> 0)` + opacity 0->1
  - Duration 300ms, ease [0.32, 0.72, 0, 1]

**Step 1: Create workspace**
- Heading: "Name your workspace" (`text-2xl font-semibold tracking-tight`)
- Subheading: "This is where your team and AI agents will live" (`text-sm text-muted-foreground mt-2`)
- Workspace name input (autofocused)
- Slug preview below: shows generated slug from name (lowercase, hyphens), `text-xs text-muted-foreground` -- "/t/{slug}"
- CTA: "Create workspace" (h-11, full width, btn-gradient-border)
- On submit: calls `createTenant({ name, slug })`
- Success: advance to step 2 with slide transition
- Error: inline error (slug taken, etc.)

**Step 2: Invite your team (optional)**
- Heading: "Invite your team" (`text-2xl font-semibold tracking-tight`)
- Subheading: "Add teammates so you can build together. You can always do this later." (`text-sm text-muted-foreground mt-2`)
- Email input: can add multiple comma-separated emails
- Role select: defaults to "member"
- CTA: "Send invites" (h-11)
- Skip: "Skip invites for now" ghost link below CTA (`text-sm text-muted-foreground hover:text-foreground`)
- On submit: calls `createInvitation` for each email
- On skip or success: advance to step 3

**Step 3: You're ready**
- Success animation: `CheckCircle2` icon with `motion.div` scale spring (0->1, stiffness 400, damping 30), `text-primary h-12 w-12`
- Heading: "You're all set" (`text-2xl font-semibold tracking-tight`)
- Body: "**{workspace name}** is ready. Start by creating your first AI agent." (`text-sm text-muted-foreground mt-2`)
- Summary: workspace name, number of invites sent
- CTA: "Open your dashboard" (h-11, primary, btn-gradient-border) -> `router.push('/t/{slug}/dashboard')`

**Onboarding page** `packages/web/app/onboarding/page.tsx`:
- `'use client'` page
- Renders `<AuthLayout><OnboardingWizard /></AuthLayout>`
- metadata: `{ title: 'Get started | AgentForge' }`
  </action>
  <verify>
    <automated>cd /Users/kevinrichards/Projects/agentforge/packages/web && npx tsc --noEmit 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - packages/web/app/(tenant)/t/[slug]/members/page.tsx contains `Team members`
    - packages/web/app/(tenant)/t/[slug]/members/page.tsx contains `InviteForm`
    - packages/web/app/(tenant)/t/[slug]/members/page.tsx contains `MemberList`
    - packages/web/components/tenant/member-list.tsx contains `usePermission('member:read')`
    - packages/web/components/tenant/member-list.tsx contains `staggerChildren`
    - packages/web/components/tenant/member-list.tsx contains `Skeleton` or loading skeleton
    - packages/web/components/tenant/member-row.tsx contains `h-14`
    - packages/web/components/tenant/member-row.tsx contains `hover:bg-zinc-800/30`
    - packages/web/components/tenant/member-row.tsx contains `listItem`
    - packages/web/components/tenant/member-row.tsx contains `Remove`
    - packages/web/components/tenant/invite-form.tsx contains `usePermission('tenant:invite')`
    - packages/web/components/tenant/invite-form.tsx contains `AnimatePresence`
    - packages/web/components/tenant/invite-form.tsx contains `createInvitation`
    - packages/web/components/tenant/invite-form.tsx contains `createInviteLink`
    - packages/web/components/tenant/invite-form.tsx contains `Invite link copied`
    - packages/web/app/invite/[token]/page.tsx contains `acceptInvitation`
    - packages/web/app/invite/[token]/page.tsx contains `Welcome to`
    - packages/web/app/invite/[token]/page.tsx contains `This invitation has expired`
    - packages/web/app/invite/[token]/page.tsx contains `Invitation not found`
    - packages/web/components/onboarding/wizard.tsx contains `Name your workspace`
    - packages/web/components/onboarding/wizard.tsx contains `Invite your team`
    - packages/web/components/onboarding/wizard.tsx contains `You're all set`
    - packages/web/components/onboarding/wizard.tsx contains `createTenant`
    - packages/web/components/onboarding/wizard.tsx contains `AnimatePresence`
    - packages/web/components/onboarding/wizard.tsx contains `mode="wait"`
    - packages/web/components/onboarding/wizard.tsx contains `bg-primary` (step dot)
    - packages/web/components/onboarding/wizard.tsx contains `Skip invites for now`
    - packages/web/app/onboarding/page.tsx contains `OnboardingWizard`
    - packages/web/app/onboarding/page.tsx contains `AuthLayout`
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>Member management page with permission-gated list (staggered animation), inline remove confirmation (no modal), expandable invite form with email and link modes, toast feedback for all async operations. Invite acceptance page handles success/expired/invalid/unauthenticated states. 3-step onboarding wizard with step slide transitions, progress bar, workspace creation, optional team invites, and success animation.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Visual and functional verification of tenant UI + onboarding</name>
  <what-built>Complete tenant dashboard with sidebar navigation, tenant switcher, member management, invitation system, onboarding wizard, and toast notifications</what-built>
  <how-to-verify>
    1. Start dev server: `cd packages/web && npm run dev`
    2. Navigate to http://localhost:3000/onboarding (simulate post-signup)
       - VERIFY: Split-screen layout (same as auth pages)
       - VERIFY: Step indicator (3 dots, first filled blue)
       - VERIFY: Progress bar (thin blue line, ~33% width)
       - VERIFY: Step 1 "Name your workspace" with name input and slug preview
       - VERIFY: Steps animate with slide transition (current slides left, next slides in from right)
    3. If Supabase running: complete onboarding flow, then navigate to tenant dashboard
    4. Navigate to http://localhost:3000/t/test-tenant/dashboard (or created tenant slug)
       - VERIFY: Sidebar on left (240px, dark zinc-900 background)
       - VERIFY: Logo + "AgentForge" at top of sidebar
       - VERIFY: Tenant switcher button below logo
       - VERIFY: Nav items: Dashboard (active with blue left accent bar), Agents, Members, Settings
       - VERIFY: Active nav item has left blue bar (0.5px wide, h-5)
       - VERIFY: Sticky header with backdrop blur at top of main content
       - VERIFY: Welcome card with blue-to-violet gradient background
       - VERIFY: "Create an agent" button is disabled/greyed out
    5. Click tenant switcher
       - VERIFY: Dropdown animates in (spring animation)
       - VERIFY: Current tenant has checkmark
       - VERIFY: "New workspace" option at bottom with plus icon
    6. Navigate to Members page via sidebar
       - VERIFY: "Team members" heading with member count badge
       - VERIFY: "Invite member" button (blue, right-aligned)
       - VERIFY: Click "Invite member" -- form expands with animation
       - VERIFY: Email input + role select + "Send invitation" button
       - VERIFY: "Or share invite link" toggle below
    7. Resize to mobile (< 768px)
       - VERIFY: Sidebar hidden, hamburger menu in header
    8. Run: `cd packages/web && npx tsc --noEmit` -- zero errors
    9. Run: `cd packages/web && npm test` -- all pass
  </how-to-verify>
  <resume-signal>Type "approved" or describe visual/functional issues</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| URL slug -> Tenant layout | Tenant slug from URL used for DB lookup |
| URL token -> Invite page | Invitation token from URL used for acceptance |
| Browser form -> Server action | User input (email, tenant name, role) crosses trust boundary |
| Client permission check -> UI rendering | usePermission determines visible actions |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-17 | Tampering | Tenant slug in URL | mitigate | Layout validates slug against DB; notFound() for invalid slugs; RLS prevents cross-tenant data access |
| T-02-18 | Elevation of Privilege | Invite form | mitigate | usePermission('tenant:invite') gates the UI; role options come from DB not user input; RBAC checked server-side in createInvitation (Plan 03) |
| T-02-19 | Elevation of Privilege | Member role change | mitigate | usePermission('member:manage') gates the UI; actual role change validated server-side with RLS policies |
| T-02-20 | Spoofing | Invitation acceptance | mitigate | Token validated via DB lookup + status + expiry + max_uses checks in acceptInvitation (Plan 03); unauthenticated users redirected to login |
| T-02-21 | Information Disclosure | Member list | mitigate | usePermission('member:read') gates the list; Supabase RLS ensures tenant-scoped queries; member emails shown only to authorized users |
| T-02-22 | Tampering | Onboarding wizard slug | mitigate | Slug generated from name via sanitization (lowercase, hyphens only); uniqueness checked server-side in createTenant; Supabase unique constraint on slug |
</threat_model>

<verification>
1. `cd packages/web && npx tsc --noEmit` -- zero TypeScript errors
2. `cd packages/web && npm test` -- all tests pass
3. `grep "TenantProvider" packages/web/app/(tenant)/t/\\[slug\\]/layout.tsx` -- context wired
4. `grep "before:bg-primary" packages/web/components/tenant/sidebar-nav.tsx` -- active nav accent bar
5. `grep "dropdownEnter" packages/web/components/tenant/tenant-switcher.tsx` -- dropdown animation
6. `grep "AnimatePresence" packages/web/components/tenant/invite-form.tsx` -- expandable form
7. `grep "staggerChildren" packages/web/components/tenant/member-list.tsx` -- stagger animation
8. `grep "acceptInvitation" packages/web/app/invite/\\[token\\]/page.tsx` -- invite wired
9. `grep "createTenant" packages/web/components/onboarding/wizard.tsx` -- onboarding creates tenant
10. `grep "border-l-4" packages/web/components/ui/toast-provider.tsx` -- toast variants styled
</verification>

<success_criteria>
- Tenant layout loads tenant from DB, provides TenantContext, renders sidebar + main content
- Sidebar: 240px width, zinc-900 bg, nav items with active left accent bar (before:absolute before:left-0 before:h-5 before:w-0.5 before:bg-primary)
- Sticky main header with backdrop-blur
- Tenant switcher: dropdown with dropdownEnter animation, tenant list with checkmark, "New workspace" option
- Dashboard: welcome card with gradient, disabled "Create agent" CTA, "Invite team" CTA
- Members page: heading + count badge, expandable invite form, permission-gated member list
- Member list: staggered animation, skeleton loader, inline remove confirmation, role badges
- Invite form: email mode + link mode with copy-to-clipboard + toast feedback
- Invite acceptance: success/expired/invalid states with correct copy
- Onboarding wizard: 3 steps with slide transitions, progress bar, step dots
- Toast system: success (#22C55E), error (#EF4444), info (#3B82F6) with slide-up animation
- Avatar: initials fallback with color-hashed backgrounds
- Empty state: centered layout for member list
- All components permission-gated via usePermission hooks
- Human verifies visual appearance matches UI-SPEC
</success_criteria>

<output>
After completion, create `.planning/phases/02-core-infrastructure/02-05-SUMMARY.md`
</output>
